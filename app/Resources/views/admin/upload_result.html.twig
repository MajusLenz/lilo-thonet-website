{% extends 'base.html.twig' %}

{% block body %}

    <a class="logout-btn" href="{{ path('logout') }}" style="float: right;">logout</a>

    <br/>
    <h1>Administrator-Bereich:</h1>

    <a href="{{ path('admin') }}">Zurück zur Uploadseite</a>
    <br/><br/>

    <h2>Error-Liste:</h2>
    <p>
        {% for error in errorList %}
            {{ error }}
            <br/><br/>
        {% endfor %}
    </p>
    <br/><br/>

    <a href="{{ path('admin') }}">Zurück zur Uploadseite</a>

    <br/><br/>

{% endblock %}
